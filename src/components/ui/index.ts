import { cn } from '@/lib/utils';
import { type ClassValue } from 'clsx';

// Simple cn without clsx dependency — just join truthy strings
// (clsx is not in our deps, so we do it manually)
export { cn };
